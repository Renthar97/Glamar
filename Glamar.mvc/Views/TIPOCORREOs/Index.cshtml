@model IEnumerable<Entidad.TIPOCORREO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CORREO.Correo1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoCorreo1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CORREO.Correo1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCorreo1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoCorreoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoCorreoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoCorreoId })
        </td>
    </tr>
}

</table>
